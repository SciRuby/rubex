lib math do
  double cos(double)

  struct exception do
    int type
    char *name
  end

  double pow(double, double)

  alias exec = struct exception
end

def maths(double a, double b)
  alias int_64 = i64
  i64 p = cos(a)
  int_64 rr
  
  rr = 332

  exec e
  e.type = 3

  struct new_struct do
    i32 a,b,c
    char* str
  end

  return pow(p, b)
end
