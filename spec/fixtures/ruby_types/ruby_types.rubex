class RubyTypes
  def these_types(str string, arr array, hsh h)
    string.concat("a")
    array.push(44)

    print(string[0])
    print(array[0])

    print(h["fff"])
    h["fff"] = 565
    print(h["fff"])

    return h
  end
end
